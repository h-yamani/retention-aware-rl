algorithm: SAC

sac:
  learning_rate: 0.0003
  buffer_size: 1000000
  learning_starts: 10000
  batch_size: 256
  tau: 0.005
  gamma: 0.99
  train_freq: 1
  gradient_steps: 1
  ent_coef: auto
  target_entropy: auto
  net_arch:
    - 256
    - 256

vem:
  capacity: 20

retention:
  checkpoint_interval_steps: 10000
  revisit_rollouts: 5

experiment:
  intervention_enabled: false
